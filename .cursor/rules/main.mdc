---
description: TixXinBlog 项目级架构约束
alwaysApply: true
---

# TixXinBlog 项目规则

- 项目当前处于 **UI 打磨阶段**，前端使用 mock 数据驱动，不接入真实后端 API，不实现服务端逻辑。
- 架构设计需为后续 API 替换预留空间（组件通过 props 接收数据，不直接依赖 mock）。
- 当前仓库仍处于"先设计、后实现"阶段，新增结构前优先遵循现有文档。
- 将 `docs/project-architecture.md` 视为当前项目的统一架构基线文档。
- 保持顶层边界稳定：
  - 前端代码放在 `src/frontend/`
  - 后端代码放在 `src/backend/`
  - 文档和设计说明放在 `docs/`
- 优先做最小、渐进式实现，避免过度预埋和推演式扩展。
- 如果实现必须偏离文档，偏离范围要小，并明确说明原因。
- 已有明确归属时，不要再发明一套平行目录约定。
- 保持模块边界清晰，不要把前端、后端、内容和文档混放到同一位置。
- 优先采用模块化开发，按领域和职责拆分代码，而不是把多类职责堆到单个文件或目录中。
- 尽量遵守单一职责原则，一个文件只做一件事；当文件开始同时承担页面、状态、请求、转换等多种职责时，应主动拆分。
- 写代码时优先考虑长期维护成本，保持命名清晰、结构稳定、依赖关系简单。
- 优先采用组件化、模块式实现，避免复制粘贴式开发。
- 遇到重复逻辑时，优先提炼为可复用组件、组合式函数、工具函数或业务模块。
- 保持仓库整洁，遵守现有 `LF` 行尾约定，避免无意义的格式噪音。
- Git 提交消息遵循 Conventional Commits 规范，描述使用中文，详见 `.cursor/rules/git-commit-message.mdc`。

## pnpm 命令执行约定

- **pnpm 命令需由用户手动执行**：Agent 不得擅自调用终端执行 `pnpm install`、`pnpm run xxx`、`pnpm add` 等 pnpm 相关命令。
- **仅可提示**：需要安装依赖、运行脚本或执行 pnpm 操作时，只输出提示文案，告知用户应执行的命令。
- **例外**：若用户在当前对话中明确表示「可以帮我执行」「你直接运行」等，则可在该对话内代为执行 pnpm 命令。

## 文件头注释规范

- 所有代码文件（`.vue`、`.ts`、`.scss`）顶部必须包含统一的文件头注释。
- `.vue` 文件使用 HTML 注释格式，放在 `<template>` 标签之前：
  ```
  <!--
    @file 文件名.vue
    @description 用中文一句话描述文件职责
    @author TixXin
    @since YYYY-MM-DD
  -->
  ```
- `.ts`、`.scss` 文件使用 JSDoc 注释格式：
  ```
  /**
   * @file 文件名（含扩展名）
   * @description 用中文一句话描述文件职责
   * @author TixXin
   * @since YYYY-MM-DD
   */
  ```
- 新建文件时必须包含文件头注释，`@since` 填写当天日期。

## 注释语言规范

- 代码中需要注释时统一使用中文。
- 仅在必要时添加注释，不做逐行翻译式注释。

## 目录结构文档

- `docs/directory-structure.md` 记录项目完整目录结构及各文件职责说明。
- 新增、删除或重命名文件/目录时，需同步更新该文档。
